{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Plakar K8S Importer Connector Config",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "location"
  ],
  "properties": {
    "location": {
      "type": "string",
      "minLength": 1,
      "description": "endpoint"
    },
    "kubeconfig": {
      "type": "string",
      "description": "Kube config content to use"
    },
    "skipped_group_kinds": {
      "type": "string",
      "description": "Optional semicolon-separated list of group/Kind to leave out of the restore, e.g. \"apps/Deployment;cert-manager.io/Certificate;/ConfigMap\". The group is the full API group, empty for the core group, the kind is CamelCase."
    }
  }
}
